using UnityEngine;

public class KrakIdleBehaviour : StateMachineBehaviour
{
    /*
    override public void OnStateUpdate(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    {
        if (DialogManager.Instance.GetDialogActive() &! animator.GetBool("isTalking"))
        {
            animator.SetBool("isTalking", true);
        }
        else if (!DialogManager.Instance.GetDialogActive() && animator.GetBool("isTalking"))
        {
            animator.SetBool("isTalking", false);
        }
    }
    */
}
