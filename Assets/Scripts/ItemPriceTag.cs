using UnityEngine;

public class ItemPriceTag : MonoBehaviour
{
    /*
    public TextMeshProUGUI priceTag;
    public Item item;
    int price;
    Item.Type itemType;
    private void Start()
    {
        itemType = item.GetItemType();
        switch (itemType)
        {
            case Item.Type.Empty:
                break;
            case Item.Type.AetherCrystal:
                SetPriceTag(9999);
                break;
            case Item.Type.EnvoyRune:
                SetPriceTag(9999);
                break;
            case Item.Type.FlameVial:
                SetPriceTag(3000);
                break;
            case Item.Type.Key:
                SetPriceTag(1000);
                break;
            case Item.Type.NauticShard:
                SetPriceTag(3000);
                break;
            case Item.Type.Stone:
                SetPriceTag(100);
                break;
            case Item.Type.Shield:
                SetPriceTag(500);
                break;

        }
    }

    void SetPriceTag(int _price)
    {
        price = _price;
        priceTag.text = itemType.ToString() + "\n" + price.ToString() + "G";
    }
    public int GetPrice()
    {
        if (price == 0)
        {
            Debug.Log("NO PRICE SET!!");
            return 9999;
        }
        else
        {
            return price;
        }
    }
    */
}
