using UnityEngine;

public class TrailMover : MonoBehaviour
{
    /*
    Vector2 pos;
    GameObject player;

    float randomAddX;
    float randomAddY;
    float perlinNoise;

    private void Start()
    {
        player = GameObject.FindGameObjectWithTag("Player");
    }

    private void Update()
    {
        if (player == null)
        {
            player = GameObject.FindGameObjectWithTag("Player");
        }
        perlinNoise = Mathf.PerlinNoise(player.transform.position.x, player.transform.position.y);
    }

    private void FixedUpdate()
    {
        if (player == null)
        {
            player = GameObject.FindGameObjectWithTag("Player");
        }
        pos.x = player.transform.position.x + perlinNoise;
        pos.y = player.transform.position.y + perlinNoise;

        transform.position = new Vector2(pos.x, pos.y);
    }
    */
}
