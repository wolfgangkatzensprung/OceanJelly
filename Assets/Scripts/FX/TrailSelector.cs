using UnityEngine;

public class TrailSelector : MonoBehaviour
{
    /*
    public static TrailSelector Instance;

    public GameObject fireTrail;
    public GameObject waterTrail;
    public GameObject defaultTrail;

    private void Awake()
    {
        Instance = this;
    }
    private void Start()
    {
        UpdateTrail(0); //reset
    }

    public void UpdateTrail(int currentAura)
    {
        TurnOffTrails();

        switch (currentAura)
        {
            case 0:
                TurnOffTrails();
                break;
            case 1:
                fireTrail.SetActive(true);
                break;
            case 2:
                waterTrail.SetActive(true);
                break;
        }
    }

    public void TurnOffTrails()
    {
        fireTrail.SetActive(false);
        waterTrail.SetActive(false);
        defaultTrail.SetActive(false);
    }
    */
}
